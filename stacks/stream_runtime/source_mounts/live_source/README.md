# live_source stream runtime mount

이 디렉터리는 stream runtime stack에서 외부 live source archive를 읽기 전용으로 mount하기 위한 계약 경로입니다.
실제 데이터 payload는 repository에 포함하지 않습니다.
원본 수집 근거는 `incoming/raw_snapshots/`에 보존합니다.
