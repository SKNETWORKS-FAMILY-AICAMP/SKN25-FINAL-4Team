apiVersion: 1

datasources:
  - name: CMS Stream Prometheus
    uid: prometheus-cms-stream
    type: prometheus
    access: proxy
    url: ${GRAFANA_PROMETHEUS_URL}
    isDefault: false
    jsonData:
      timeInterval: 15s
      queryTimeout: 60s
