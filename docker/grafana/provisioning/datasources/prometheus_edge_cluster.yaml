apiVersion: 1

datasources:
  - name: CMS Local PC123 Prometheus
    uid: prometheus-edge-cluster
    type: prometheus
    access: proxy
    url: ${GRAFANA_PROMETHEUS_URL}
    isDefault: false
    jsonData:
      timeInterval: 15s
      queryTimeout: 60s
